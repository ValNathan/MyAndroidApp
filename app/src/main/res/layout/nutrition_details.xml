<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
                                                   android:layout_width="match_parent"
                                                   android:layout_height="match_parent"
                                                   xmlns:app="http://schemas.android.com/apk/res-auto">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/nutrition_detail_titre"
        android:text="Repères nutritionnels pour 100g"
        android:textSize="15sp"
        android:textColor="@color/black"
    />

    <TextView android:layout_width="20dp" android:layout_height="20dp"
        android:id="@+id/lipides"
        android:background="@drawable/circles"

        app:layout_constraintTop_toBottomOf="@id/nutrition_detail_titre"
    />

</androidx.constraintlayout.widget.ConstraintLayout>